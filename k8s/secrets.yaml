# This file is a TEMPLATE. Do NOT commit real values.
# In production, create the secret from Vault or via CI:
#
#   kubectl create secret generic stas-secrets \
#     --namespace stas \
#     --from-literal=NEO4J_PASSWORD=<value> \
#     --from-literal=ANTHROPIC_API_KEY=<value> \
#     --from-literal=CLERK_JWKS_URL=<value> \
#     --from-literal=SENTRY_DSN=<value> \
#     --from-literal=NEXT_PUBLIC_SENTRY_DSN=<value> \
#     --from-literal=GITHUB_WEBHOOK_SECRET=<value> \
#     --from-literal=SLACK_SIGNING_SECRET=<value> \
#     --from-literal=JIRA_WEBHOOK_SECRET=<value>
#
apiVersion: v1
kind: Secret
metadata:
  name: stas-secrets
  namespace: stas
type: Opaque
stringData:
  NEO4J_PASSWORD: "REPLACE_IN_VAULT"
  ANTHROPIC_API_KEY: "REPLACE_IN_VAULT"
  CLERK_JWKS_URL: "REPLACE_IN_VAULT"
  SENTRY_DSN: "REPLACE_IN_VAULT"
  NEXT_PUBLIC_SENTRY_DSN: "REPLACE_IN_VAULT"
  GITHUB_WEBHOOK_SECRET: "REPLACE_IN_VAULT"
  SLACK_SIGNING_SECRET: "REPLACE_IN_VAULT"
  JIRA_WEBHOOK_SECRET: "REPLACE_IN_VAULT"
