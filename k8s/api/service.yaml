apiVersion: v1
kind: Service
metadata:
  name: stas-api
  namespace: stas
  labels:
    app: stas-api
spec:
  selector:
    app: stas-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
